---
users:
  - { name: 'wbonis', sshkeys: 'https://github.com/wbonis.keys' }

letsencrypt_email: wb@stylite.de

mail_admin:
  { server: 'mail.stylite.eu', port: '25', domain: 'stylite.de',
    user: '{{ vault_mail_admin_user }}"', auth: 'plain', starttls: 'false', sender: 'noreply@stylite.de' }

mail_admin_password: "{{ vault_mail_admin_password }}"
greenlight_key_base: "{{ vault_greenlight_key_base }}"

greenlight_postgres_password: "{{ vault_greenlight_postgres_password }}"

greenlight_admin_user: "{{ vault_greenlight_admin_user }}"
greenlight_admin_email: "{{ vault_greenlight_admin_email }}"
greenlight_admin_password: "{{ vault_greenlight_admin_password }}"

scalelite_loadbalancer_url: "https://video.bonis.info/bigbluebutton/"
scalelite_loadbalancer_secret:  "{{ vault_scalelite_loadbalancer_secret }}"

monitoring_grafana_admin_password: "{{ vault_monitoring_grafana_admin_password }}"

monitoring_github_client_id: wbonis

monitoring_github_client_secret:  "{{ vault_monitoring_github_client_secret }}"

turn_secret: "{{ vault_turn_secret }}"